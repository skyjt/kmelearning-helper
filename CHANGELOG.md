# 更新日志（Changelog）

本文件记录 KME 学习助手（kmelearning-helper / KME Learning Navigator）各版本的主要变化。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [2.12.1] - 2026-07-26

### 变更（Changed）
- 测验结果识别改为先用整页文本做关键词粗筛，只有出现「通过 / 合格 / 很遗憾 / 未达到」等字样时才逐元素扫描；提交后每 250ms、等待结果时每 400ms 的轮询不再对全页 `div/p/span` 反复执行 `getComputedStyle` 和 `innerText`。粗筛条件是原有匹配规则的严格超集，判定结果不变。
- 回填答案时只提取一次题目，之后按题号复用，仅在页面真的替换了待点击节点时才重新提取；此前每个选项点击前都要重走一遍全量题目解析，20 题的试卷约需 40 次。
- `visible()` 与 `textOf()` 共用同一套按元素缓存（同样的 DOM 版本号 + 200ms 兜底），避免同一轮扫描里对相同元素重复触发样式计算与回流。

## [2.12.0] - 2026-07-24

### 修复（Fixed）
- 修复浮窗多处 `hidden` 属性被 CSS 网格/弹性布局覆盖的问题：全自动答题模式下「应用高置信/全部答案」按钮不再错误显示，确认学习项目时任务列表会正确收起，空答题结果列表不再残留间距。
- 统一课程目录页路径识别：无尾斜杠的 `/home/training/study` 地址不再只依赖页面文本兜底。
- 自动学习主循环增加异常兜底：单个处理环节抛出意外错误时记录日志并在下一周期重试，不再产生未捕获异常。
- 视频补学重试耗尽后记住当前视频，不再每个周期空等数秒重复无效流程；重新开始学习时重置该状态。
- 视频结束后先等待平台完成确认，再刷新「记录」并按目录实际视频时长核对；完整播放且平台记录已达标时直接进入测验，避免完成勾选延迟造成整段归零重播。
- 课程结束或进入下一门课程时清空上一门的时长摘要，浮窗不再显示跨课程残留数据。
- 「返回」按钮查找排除学习助手自身浮窗，并收紧为「返回」开头的前缀匹配。
- 扩展域名守卫锚定完整域名边界；模型接口请求失败文案不再向扩展用户提示油猴设置；刷新学习记录减少一次重复的存储写入。
- 烟雾测试新增「全自动模式下应用答案按钮真正隐藏」「Esc 取消项目确认」以及「48:06 视频已完整播放、记录 01:21:49 时不重播并进入测验」的回归场景。

### 变更（Changed）
- 浮窗正面增加与设置面一致的高度上限和内部滚动，小窗口下内容不再溢出屏幕。
- 状态行最多显示两行，完整内容悬停可见，减少卡片高度跳动；状态区支持屏幕阅读器播报。
- 右下角悬浮球改为仅点击展开，取消划过即展开，避免误触；待学项目确认弹窗支持按 Esc 取消。
- 后台标签页只做标签标题进度同步，跳过不可见面板的整页扫描；答题区题目提取按扫描周期缓存，面板定时刷新不再重复解析整套题目。

## [2.11.1] - 2026-07-24

### 修复（Fixed）
- 修复课程页面切换后，已经离开页面的旧播放器可能留下永久不返回的播放请求，导致自动学习主循环一直停在忙碌状态的问题。
- 播放、重播和补学路径新增播放器连接状态检查、页面切换检测与 5 秒超时；播放器失效时会重新识别当前页面并继续后续流程。
- 扩展存储读写增加 3 秒兜底超时，避免浏览器回调异常时阻塞自动学习。
- 测验结果优先识别明确的通过证据；明确失败信息需稳定显示 2.5 秒后才停止，降低平台分阶段渲染造成的误判。
- 修复测验是课程目录最后一项时，通过后又绕回旧视频、反复出现课程提醒并返回已通过测验的循环。
- 最终测验通过后以目录实际视频时长核对学习记录，避免把“1 学时”课程积分误当成 3600 秒播放要求，再次打开已完成视频。
- Chrome 扩展与油猴脚本烟雾测试新增“旧播放器播放请求永久挂起后切换到测验”“失败结果稳定显示”以及“1 学时课程的 95 分最终测验通过后直接返回”的回归场景。

## [2.11.0] - 2026-07-24

### 新增（Added）
- 自动学习运行时，浏览器标签页标题会显示「【自动学习中 已完成/总数·百分比】+ 原页面标题」，不用点开页面就能在标签栏看到学习进度；停止自动学习后恢复页面原标题。
- 平台页面自己改写标题时（单页应用跳转），会自动以新标题为基准继续显示进度。
- 双版本烟雾测试新增标签标题运行中与进度断言。

## [2.10.1] - 2026-07-23

### 修复（Fixed）
- 修复课程目录中的测验名称使用“测试题”时未被识别，导致视频完成后直接返回上一级的问题。
- 目录候选项改为复用统一的题目关键词识别，并为真实“《树立和践行正确政绩观》（下）测试题”名称补充双版本烟雾测试。

## [2.10.0] - 2026-07-23

### 变更（Changed）
- 统一浮窗层级术语：首页列表改称「待学项目」，进入项目后进度条改称「课程进度 x/y」；底部摘要中的「目录 / 内容」改称「课程 / 小节」，避免"首页显示 1 个任务、点进项目变 2 个"的口径混乱。
- 首页底部摘要不再重复项目数量，改为提示"点击项目并确认后，将自动逐门学习其中未完成的课程"；进入项目、列表刷新、卡片点击失败等相关状态与确认弹窗文案同步改为"项目"。
- 首页隐藏「开始自动学习 / 立即检查」大按钮行（在首页点击没有实际作用）；任务区标题栏新增「刷新」小按钮，承接原有的手动重扫功能。
- 任务行右侧与标题区重复的"项目"徽章不再显示；任务标题超长截断时，悬停可查看完整名称。
- 双版本烟雾测试同步更新首页与进度条断言文案。

## [2.9.1] - 2026-07-23

### 修复（Fixed）
- 修复测验结果页分阶段渲染时，仅先出现“再考一次”就被误判为未通过、导致全自动学习停机的问题。
- 结果页现在会继续等待明确的通过、未通过或成绩证据，再决定后续动作。
- 双版本烟雾测试新增“先显示再考一次、稍后显示通过结果”的真实页面时序回归场景。

## [2.9.0] - 2026-07-23

### 新增（Added）
- 新增「全自动答题并提交」模式：自动关闭测验须知、调用模型、填写全部有效答案、点击平台提交按钮并处理确认弹窗。
- 测验通过后自动进入下一项或下一门课程，实现视频学习到答题提交再到后续课程的连续流程。
- 测验通过后仍会校验课程总学习时长；不足时先返回视频按 1x 补学，达标后再进入下一门。
- 模型分析失败时最多请求两次；答案不完整、图片题、回填失败、未通过或结果超时会停止自动学习，防止重复提交。

### 变更（Changed）
- AI 主开关更名为「AI 自动答题」；关闭全自动开关后仍可使用答案预览、分级回填和人工提交。
- 双版本烟雾测试新增全自动提交、确认弹窗、低置信答案、通过后续学、动作去重和失败停机验证。

## [2.8.1] - 2026-07-23

### 修复（Fixed）
- 修复测验须知中的“合格分数”被误判为测验已完成，导致自动学习离开答题页的问题。

## [2.8.0] - 2026-07-23

### 新增（Added）
- 新增 AI 答题辅助，可配置 OpenAI-compatible 接口地址、API Key 和模型名称，并提供连接测试。
- 自动提取页面可见的单选题、多选题和判断题，一次性请求模型并以结构化列表展示选项、置信度和简短理由。
- 支持分别应用高置信答案或全部有效答案；只负责勾选页面选项，最终提交继续使用平台原生确认。
- API Key 默认仅保留在当前页面会话；用户主动开启「记住 API Key」后才写入油猴或扩展本地存储。
- 油猴版增加 `GM_xmlhttpRequest` 与 `@connect` 支持，用于访问用户自行配置的跨域模型接口。

### 变更（Changed）
- 「AI 答题辅助」与「跳过做题页」改为互斥模式，自动学习遇到测验时可停留并等待用户启动 AI 分析。
- 双版本烟雾测试增加模型配置、题目提取、请求内容、低置信答案、选项回填、异常响应和禁止自动提交验证。

## [2.7.1] - 2026-07-23

### 修复（Fixed）
- 修复课程视频播放完成后可能读取到旧的「学习总时长」，进而误判时长没有增长、重复补学的问题。
- 校验课程总时长前会重新打开「记录」页，并等待异步请求返回真实数据；记录页初始显示的 `00:00:00` 不再被直接用于判断。
- 双版本烟雾测试新增记录页刷新回归场景，覆盖初始零时长、加载状态和刷新后真实时长解析。

## [2.7.0] - 2026-07-23

### 新增（Added）
- 登录首页后自动识别「我的任务」中的未完成任务，并在学习助手浮窗内显示可选择的任务列表。
- 点击任务会先显示二次确认；确认后依次进入任务详情、点击「进入学习」，再从课程目录开始自动学习。
- 双版本烟雾测试新增首页任务检测、已完成任务过滤、取消确认和完整任务导航链路。

### 变更（Changed）
- 首页检测到未完成任务时自动展开学习助手；用户手动最小化后，同一页面内不会反复弹出。
- 浮窗主按钮在首页提示先选择任务，「立即检查」可在未启动状态下刷新任务列表。

## [2.6.0] - 2026-07-23

### 新增（Added）
- 新增可直接安装的 `userscript/kme-learning-helper.user.js`，油猴版与 Chrome 扩展共用自动学习核心、浮窗样式和图标。
- 新增油猴构建命令和产物一致性检查；版本、样式、图标或核心逻辑变更后可自动重新生成单文件。
- Playwright 烟雾测试增加油猴注入模式，完整流程会分别验证 Chrome 版与油猴版。

### 变更（Changed）
- 页面增加跨运行环境的加载标记，误开两个版本时只启动一套自动学习循环。
- 安装和开发文档增加油猴版的安装、更新与构建说明。

## [2.5.0] - 2026-06-07

### 变更（Changed）
- 行为开关收进浮窗的**翻转设置面**：点右上角 **⚙**，卡片会 3D 翻转到背面（设置），点「**完成**」再翻回正面。正面只保留「开始 / 停止自动学习」「立即检查」和进度显示，更清爽。
- 设置面包含四项开关：自动播放、未完成自动补学、总时长达标再返回、跳过做题页；改动**即时生效**。
- 浮窗的「总进度」只在**课程目录页**或**自动学习进行中**显示；在其它页面或未学习时不再显示上次留下的残留进度。

### 移除（Removed）
- 去掉「**锁定 1x 真实学习**」开关。视频本就按平台规则固定以 1x 播放，该开关没有实际意义；1x 现为自动学习的固定核心行为，不再可关闭。

## [2.4.1] - 2026-06-06

### 修复（Fixed）
- 修复课程目录里**同名课程被合并**、导致总数少算的问题。去重逻辑原先只按文字判重，会把标题完全相同的两门课（例如两门「征信合规管理」）当成一门，进度条因此显示 `5/6` 而非 `6/6`。现改为按「文字 + 屏幕位置」区分：同名但位于不同位置的课程会分别保留与计数；嵌套/重叠的同一行仍会被正确合并。

### 已知限制（Known issue）
- 课程完成状态目前仍按**标题**记录，因此两门完全同名的课程在「自动学习」中还不能分别追踪——第二门可能被当成已完成而跳过。计划在后续版本改为「标题 + 出现次序」识别。

## [2.4.0] - 2026-06-06

### 变更（Changed）
- 浮窗不再在所有 `pc.kmelearning.com` 页面默认弹出，改为**默认最小化成右下角的图标**。
- 仅当进入学习地图的**课程目录页**（路径 `…/home/training/study`）时，浮窗才会**默认自动展开**；课程播放页、答题页、首页等其它页面保持最小化，不再遮挡视频或正文。
- 平台是单页应用，自动学习在「目录 → 课程 → 返回目录」之间跳转时，浮窗会**随页面自动展开 / 最小化**；在同一页面手动展开或最小化的选择会被保留，直到再次切换页面。

### 备注
- 课程正文页归属 `…/home/course/…`，已与目录页区分，因此默认保持最小化。
- 升级后请在 `chrome://extensions` 重新加载扩展。

## [2.3.0]

### 优化
- 性能优化：对 DOM 扫描与 `innerText` 读取按需缓存（epoch / TTL），大幅减少每次轮询和面板刷新的重复开销。
- 面板局部刷新：启停状态只更新按钮与状态圆点，不再整块重建浮窗。
- 界面焕新：开关改为紧凑拨动样式、悬浮图标改用 128px 高清图标，并补充深色模式与「减少动态效果」适配。

## [2.2.0]

### 新增
- **课程总时长校验**：返回课程列表前，检查学习记录里的「学习总时长」是否达到课程要求学时，不够则按 1x 继续补学 / 重播。
- 新增「总时长达标再返回」开关。

## [2.1.0]

### 变更
- 更新安装说明与扩展元数据（核心逻辑与 2.0.0 一致）。

## [2.0.0]

### 首个公开版本
- 在学习目录页注入「学习助手」浮窗，实时显示当前目录的总进度。
- 按目录顺序自动进入未完成课程并播放，锁定 1x 真实学习。
- 视频播完但平台未确认完成时，自动重播补学。
- 自动跳过考试 / 测验 / 问卷 / 作业等需人工完成的页面（不代答、不提交）。
- 文档 / 材料页保持活跃，等待平台完成标记。
